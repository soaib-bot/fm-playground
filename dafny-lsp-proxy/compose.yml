services:
  dafny-lsp-proxy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dafny-lsp-proxy
    ports:
      - "8080:8080"
    environment:
      API_URL: http://localhost:8000
